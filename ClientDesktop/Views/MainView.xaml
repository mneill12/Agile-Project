<core:UserControlViewBase x:Class="ClientDesktop.Views.MainView"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:core="CSC3045AgileCore"
                          xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                          mc:Ignorable="d" 
                          d:DesignHeight="960" d:DesignWidth="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="1">
            <TabItem Header="{Binding DashboardViewModel.ViewTitle}" Content="{Binding DashboardViewModel}" />
            <TabItem Header="{Binding LoginRegisterViewModel.ViewTitle}" Content="{Binding LoginRegisterViewModel}" />
        </TabControl>
    </Grid>
</core:UserControlViewBase>
