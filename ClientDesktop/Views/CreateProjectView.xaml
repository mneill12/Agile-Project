<core:UserControlViewBase x:Class="ClientDesktop.Views.CreateProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientDesktop.Views"
             xmlns:core="CSC3045AgileCore">
    <Grid Height="960" Width="1280" Background="LightSlateGray" Margin="0,0,0,0">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Project Name: " />
            <TextBox Grid.Row="1" Name="ProjectNameTextBox" />

            
            <Button Grid.Row="2" Name="CreateProjectButton" Command="{Binding CreateProject}" CommandParameter="{Binding ElementName=ProjectNameTextBox}" >Create Project</Button>
        </Grid>
        
    </Grid>
</core:UserControlViewBase>
